#pragma once

// StateType 
/////////////////////////////////////////////////////////////////////////////////
enum StateType {
  STATE_MENU = 0, 
  STATE_SETTINGS, 
  STATE_GAME,
  STATE_WIN,
  STATE_LOSE,

  STATES_MAX = STATE_LOSE + 1,
};
/////////////////////////////////////////////////////////////////////////////////
